export interface NewCollectionParams {
  name: string;
  symbol: string;
  picUrl: string;
}

export interface CollectionData {
  id: string;
  name: string;
  symbol: string;
  owner: string;
  picUrl: string;
}
